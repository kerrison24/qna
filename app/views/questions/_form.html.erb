<%= form_for([@list, @list.questions.build]) do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.submit 'Submit', class: 'waves-effect waves-light btn' %>
  </p>
<% end %>
