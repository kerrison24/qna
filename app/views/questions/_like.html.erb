(<%= pluralize(Like::Like.with_likeable(question).count, 'vote') %>) |
 <% if Like::Like.liking?(current_user, question) %>
   <%= link_to 'Downvote', like_path_for(question), method: :delete %>
 <% else %>
     <%= link_to 'Upvote', like_path_for(question), method: :post %>
 <% end %>
