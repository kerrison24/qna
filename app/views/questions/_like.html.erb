(<%= Like::Like.with_likeable(question).count %>) |
 <% if Like::Like.liking?(current_user, question) %>
   <%= link_to 'Unlike', like_path_for(question), method: :delete %>
 <% else %>
     <%= link_to 'Like', like_path_for(question), method: :post %>
 <% end %>
